/*
    Dada una cadena de entrada, hay que devolver el recuento de las vocales que
    hay en la misma. 
*/

const vocal_counter = chain => {
    if(typeof chain === "undefined") throw new Error("Parameter was undefined")
}

export default vocal_counter